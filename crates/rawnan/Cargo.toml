[package]
name = "dmesh-rawnan"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[features]
default = ["std"]
std = ["anyhow/std"]

[dependencies]
anyhow = { version = "1", default-features = false }
