[package]
name = "dmesh-transport"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []

[lib]
name = "dmesh_transport"
path = "src/lib.rs"

[dev-dependencies]
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread"] }
