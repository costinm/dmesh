[Unit]
Description=DMesh Recovery TCP flash server socket

[Socket]
# Start this socket explicitly when the lab flash service is needed. There is
# intentionally no [Install] section: it is not a boot-enabled service.
ListenStream=3336
NoDelay=true
ReusePort=true

[Install]
# Deliberately empty: use `systemctl start flash-server.socket`.
